{% extends 'base.html.twig' %}

{% block title %}Gestion de planning{% endblock %}

{% block body %}
    <header class="p-3 bg-dark text-white">
        <div class="container">
            <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
                <a href="{{ path('index') }}" class="d-flex align-items-center mb-2 mb-lg-0 text-white text-decoration-none">
                    <img src="https://img.icons8.com/plasticine/100/000000/apple-calendar.png" alt="Logo calendrier"
                        width="50" height="50"/>
                </a>

                <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
                    <li><a href="{{ path('index') }}" class="nav-link px-2 text-white">Accueil</a></li>
                </ul>

                <div class="text-end">
                    {% if app.user %}
                        <a href="{{ path("dashboard") }}">
                            <button type="button" class="btn btn-outline-light me-2">
                                Dashboard
                            </button>
                        </a>
                    {% else %}
                        <a href="{{ path("app_login") }}">
                            <button type="button" class="btn btn-outline-light me-2">
                                Connexion
                            </button>
                        </a>
                    {% endif %}
                </div>
            </div>
        </div>
    </header>

    <div class="container text-center pt-5">
        <h1>Gestion de planning</h1>
        <p>Bienvenue dans l'application de gestion de planning. Veuillez vous connecter pour continuer.</p>
    </div>
{% endblock %}